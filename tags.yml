---
---
# {% capture tags %}{% for collection_item in site.paper_gods %}{% for t in collection_item.tags %}{{ t }}{% unless forloop.last %}^{% endunless %}{% endfor %}{% unless forloop.last %}^{% endunless %}{% endfor %}{% endcapture %}{% assign taglist = tags | split: "^" | uniq %}{% for t in taglist %}- {{ t }}
# {% endfor %}
